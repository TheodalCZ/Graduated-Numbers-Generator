# ArcGIS Pro Toolbox

Place the final `.atbx` file intended for publication into this folder.

Recommended contents:
- the final version of the toolbox
- optionally a short text note if you need to explain the version or dependencies

Note:
The `.atbx` file is an embedded toolbox. The standalone scripts in the `src/` folder are not necessarily a literal copy of its internal contents — they document and version the logic developed during the thesis work.